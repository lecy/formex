% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-fx-features.R
\name{fx_extract_features}
\alias{fx_extract_features}
\title{Extract the value-profile feature vector for a column}
\usage{
fx_extract_features(
  x,
  col_name = NULL,
  blocks = ALL_BLOCKS,
  lexicons = default_lexicons(),
  max_unique = 20000,
  seed = 1L
)
}
\arguments{
\item{x}{A column (coerced to character).}

\item{col_name}{Optional column name; enables the \code{header} block features.}

\item{blocks}{Character vector of feature blocks to compute (default all).
Compute only the blocks a fitted model references to trim deployment cost
(see \code{\link[=fx_feature_manifest]{fx_feature_manifest()}}, \code{\link[=fx_blocks_for_features]{fx_blocks_for_features()}}).}

\item{lexicons}{Named list of controlled vocabularies for the lexicon block
(default \code{\link[=default_lexicons]{default_lexicons()}}).}

\item{max_unique}{Cap on distinct values profiled (downsampled above it).}

\item{seed}{Integer seed for the internal, caller-non-invasive RNG stream.}
}
\value{
A one-row data frame of numeric/\code{NA} features.
}
\description{
Profiles a column into a wide, flat numeric feature row across a set of
independent, block-gated feature families (core, sequence, length,
character-class, punctuation, mask, character-frequency, regex, numeric,
token, lexicon, header). This is the substrate the learned router and the
signature primitives are built on. Extraction is reproducible (a local,
non-invasive RNG stream) so a profile is stable across runs.
}
\seealso{
\code{\link[=fx_type_signature]{fx_type_signature()}}, \code{\link[=fx_feature_manifest]{fx_feature_manifest()}}
}

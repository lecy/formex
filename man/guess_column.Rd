% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02-02-classify-by-name.R
\name{guess_column}
\alias{guess_column}
\title{Guess a column's type from its values and its name}
\usage{
guess_column(x, name = NULL, threshold = 0.8, ...)
}
\arguments{
\item{x}{A vector (coerced to character) of column values.}

\item{name}{Optional variable name (header string).}

\item{threshold}{Minimum value-detector pass rate to trust the value guess.}

\item{...}{Passed to \code{\link[=guess_data_type]{guess_data_type()}}.}
}
\value{
A list with \code{source} (\code{"value"}, \code{"name"}, or \code{"none"}), \code{guess},
\code{ontology} (the standard 4-vector), \code{confidence}, and the full \code{value} and
\code{name} sub-results for inspection.
}
\description{
Runs both detection modalities and reconciles them VALUES-FIRST: if a value
detector clears \code{threshold}, its (format-anchored) answer is returned; only
when no value detector is confident does the name classifier fill in the
class. This is what lets the metadata-gated classes -- a \code{double} column
named \code{sampling_weight}, a \code{0/1} column named \code{eligible} -- be typed at all,
while never letting a misleading name override a real value signature.
}
\examples{
guess_column(c("06037", "36061"), name = "county")$source    # "value"
guess_column(c("3.4", "5.1", "7.8"), name = "sampling_weight")$guess  # weight
}
\seealso{
\code{\link[=guess_data_type]{guess_data_type()}}, \code{\link[=classify_by_name]{classify_by_name()}}
}

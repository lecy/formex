% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-autotype-functions.R
\name{generate_autotype_test_cases}
\alias{generate_autotype_test_cases}
\title{Build a labelled test-case table for one data type}
\usage{
generate_autotype_test_cases(
  P,
  data_type,
  tiers = c("S1", "S2", "S3", "SP"),
  n_per_positive = 20,
  p = 0.5,
  seed = 514
)
}
\arguments{
\item{P}{A character vector of positive examples.}

\item{data_type}{A single string naming the type (e.g. \code{"currency_code"}).}

\item{tiers}{Mutation tiers to include.}

\item{n_per_positive}{Number of mutants to attempt per positive per tier.}

\item{p}{Per-character mutation probability.}

\item{seed}{Integer seed; set for reproducible fixtures.}
}
\value{
A data frame with columns \code{data_type}, \code{case}, and \code{label}, where
\code{label} is \code{"valid"} for real positives or the tier name (\code{"S1"}, \code{"S2"},
\code{"S3"}, \code{"SP"}) for negatives. Negatives coinciding with a real positive
are dropped.
}
\description{
Assembles the pure inputs used by the bundled \link{data_type_tests} object: the
real positive examples plus deduplicated negatives from each mutation tier,
labelled by origin. Unlike \code{\link[=build_autotype_results]{build_autotype_results()}}, this stores no
detector predictions -- it is the fixture a detector is later scored against.
}

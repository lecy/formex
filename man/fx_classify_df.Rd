% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/20-fx-deploy.R
\name{fx_classify_df}
\alias{fx_classify_df}
\title{Classify every column of a data frame under deployment rules}
\usage{
fx_classify_df(
  df,
  cols = names(df),
  n_rows = 500L,
  blocks = NULL,
  features_used = NULL,
  cardinality = c("full", "sample"),
  cascade = NULL,
  lexicons = default_lexicons(),
  threshold = 0.8,
  seed = 1L
)
}
\arguments{
\item{df}{A data frame (or list of columns).}

\item{cols}{Which columns to classify; defaults to all.}

\item{n_rows, blocks, features_used, cardinality, cascade, lexicons, threshold, seed}{Passed through to \code{\link[=fx_classify_column]{fx_classify_column()}}.}
}
\value{
A data frame with \code{column}, \code{source}, \code{guess}, \code{confidence},
\code{data_type}, \code{data_class}, \code{n}, \code{n_unique}, \code{n_sampled}. The per-column
feature rows are bound into a data frame stored in \code{attr(., "features")}.
}
\description{
Batch driver over \code{\link[=fx_classify_column]{fx_classify_column()}}. Resolves the detector list (or the
feature manifest) ONCE and reuses it across columns, then returns a tidy
one-row-per-column prediction table. Feature rows are attached as an attribute
(\code{"features"}) for callers that want them.
}
\seealso{
\code{\link[=fx_classify_column]{fx_classify_column()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-autotype-functions.R
\name{build_autotype_results}
\alias{build_autotype_results}
\title{Run a detector against positives and generated negatives}
\usage{
build_autotype_results(
  detector,
  P,
  tiers = c("S1", "S2", "S3", "SP"),
  n_per_positive = 20,
  p = 0.5,
  seed = 514,
  threshold = 0.5
)
}
\arguments{
\item{detector}{A function taking a character vector and returning a logical
(or numeric score) vector.}

\item{P}{A character vector of positive examples.}

\item{tiers}{Mutation tiers to include.}

\item{n_per_positive}{Mutants attempted per positive per tier.}

\item{p}{Per-character mutation probability.}

\item{seed}{Integer seed.}

\item{threshold}{Score threshold used when \code{detector} returns numeric.}
}
\value{
A data frame with columns \code{case}, \code{type} (\code{"valid"} or a tier),
\code{f_match} (raw detector output), and \code{f_predict} (logical prediction).
}
\description{
Scores \code{detector} on the real positives \code{P} and on negatives generated at
each mutation tier, returning one tidy table. Used to build the
human-readable artifacts in \verb{data-types/}.
}

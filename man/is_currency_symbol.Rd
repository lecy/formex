% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dt-currency.R
\name{is_currency_symbol}
\alias{is_currency_symbol}
\title{Is it a currency symbol + amount?}
\usage{
is_currency_symbol(x)
}
\arguments{
\item{x}{A character vector of candidate values.}
}
\value{
A logical vector the same length as \code{x}: \code{TRUE} where the value
matches the symbol+amount pattern, \code{FALSE} otherwise, \code{NA} where \code{x} is
\code{NA}.
}
\description{
Tests whether each element is a currency symbol immediately followed by a
numeric amount with at most two decimals, e.g. \code{"$12.30"}, \code{"€299.10"},
\code{"¥1222"}, \code{"£49.99"}. Recognised symbols are \code{$}, \verb{€} (euro),
\verb{¥} (yen), and \verb{£} (pound).
}
\details{
Rejects thousands separators (\code{"$1,000"}), more than two decimals
(\code{"$1.234"}), and a bare symbol (\code{"$"}). Extend the character class in the
regex to admit more symbols.
}
\examples{
is_currency_symbol(c("$12.30", "¥1222", "$1,000", "$", NA))
#> TRUE TRUE FALSE FALSE NA

}
\seealso{
\code{\link[=is_currency_code]{is_currency_code()}}, \code{\link[=is_currency_code_symbol]{is_currency_code_symbol()}}, \code{\link[=guess_data_type]{guess_data_type()}}

Other currency detectors: 
\code{\link{is_currency_code}()},
\code{\link{is_currency_code_symbol}()}
}
\concept{currency detectors}

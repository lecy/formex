% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/20-fx-deploy.R
\name{fx_classify_column}
\alias{fx_classify_column}
\title{Classify a single column under deployment rules (sample + gate + score)}
\usage{
fx_classify_column(
  x,
  name = NULL,
  n_rows = 500L,
  blocks = NULL,
  features_used = NULL,
  manifest = NULL,
  cardinality = c("full", "sample"),
  cascade = NULL,
  detectors = NULL,
  lexicons = default_lexicons(),
  threshold = 0.8,
  seed = 1L
)
}
\arguments{
\item{x}{A column (any vector).}

\item{name}{Optional column header, used by the detector's name tie-break and
the header feature block.}

\item{n_rows}{Row-sample size for the expensive feature blocks and detection.
A few hundred is enough for format detection; see the package notes on the
speed/coverage trade-off.}

\item{blocks}{Explicit feature blocks to compute; overrides \code{features_used}.}

\item{features_used}{Character vector of the feature names the fitted model
references; blocks are derived from these via \code{\link[=fx_feature_manifest]{fx_feature_manifest()}}.
Ignored when \code{blocks} is supplied.}

\item{manifest}{Optional precomputed \code{\link[=fx_feature_manifest]{fx_feature_manifest()}} (pass it in a
batch so it is not rebuilt per column).}

\item{cardinality}{\code{"full"} (default) recomputes the whole-column cardinality
features (\code{n_unique}, \code{unique_ratio}, \code{hapax_ratio}, ...) with one extra
linear pass, so those features are exact even though the string blocks were
sampled. \code{"sample"} skips that pass entirely -- fastest, but cardinality
features then describe the sample, not the column.}

\item{cascade}{Optional scorer: a function of the one-row feature data frame
returning a list with at least \code{guess}, \code{confidence}, and \code{ontology}. When
\code{NULL}, falls back to the rule-based \code{\link[=guess_column]{guess_column()}}.}

\item{detectors}{Named list of detector functions; pass the result of
\code{\link[=data_type_detectors]{data_type_detectors()}} once in a batch to avoid re-resolving ~90 functions
per column. Ignored when \code{cascade} is supplied.}

\item{lexicons}{Lexicon set for the lexicon block; defaults to
\code{\link[=default_lexicons]{default_lexicons()}}.}

\item{threshold}{Minimum detector pass rate to trust a value guess.}

\item{seed}{Sampling seed (features and detection stay reproducible).}
}
\value{
A list: \code{column}, \code{source}, \code{guess}, \code{confidence}, \code{ontology},
\code{features} (one-row data frame), \code{blocks}, \code{n} (usable rows seen for
cardinality), \code{n_unique}, and \code{n_sampled}.
}
\description{
The deployment entry point for one column. Draws a single row sample, runs
the block-gated feature extractor and the value/name detector over it, and
returns a prediction plus the feature row. This is the only place that should
call \code{\link[=fx_extract_features]{fx_extract_features()}} in production, because it is the only place that
guarantees the extractor never runs un-sampled.
}
\seealso{
\code{\link[=fx_classify_df]{fx_classify_df()}}, \code{\link[=fx_extract_features]{fx_extract_features()}}, \code{\link[=guess_column]{guess_column()}}
}

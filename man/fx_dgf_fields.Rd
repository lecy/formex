% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/16-fx-governance.R
\name{fx_dgf_fields}
\alias{fx_dgf_fields}
\title{Format a type prediction as governance-file (DGF) fields}
\usage{
fx_dgf_fields(
  data_type = NA,
  semantic_family = NA,
  semantic_type = NA,
  semantic_type_id = NA,
  confidence = NA_real_,
  detector_id = NA,
  ontology_version = "v6",
  ont = NULL
)
}
\arguments{
\item{data_type, semantic_family, semantic_type}{The resolved path prefix; any
suffix may be \code{NA}.}

\item{semantic_type_id}{If given (and the path is not), the path is resolved
from the ontology.}

\item{confidence}{Detector confidence in \verb{[0,1]}, or \code{NA}.}

\item{detector_id}{Identifier of the detector that produced the guess, so a
bad detector can be traced through the variables it touched.}

\item{ontology_version}{The ontology version the guess was made against;
stored so a version bump can invalidate the guess (§5.2).}

\item{ont}{Ontology table for id resolution (defaults to \code{\link[=fx_ontology]{fx_ontology()}}).}
}
\value{
A one-row data frame with \code{data_type}, \code{semantic_family},
\code{semantic_type}, \code{semantic_type_id}, \code{depth_labeled}, \code{confidence},
\code{ontology_version}, \code{detector_id}.
}
\description{
Produces the eight per-variable fields the DGF expects. Supply as much of the
path as the detector could resolve; deeper levels left \code{NA} are a legitimate
shallow (graceful) prediction, reflected in \code{depth_labeled}.
}

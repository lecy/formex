% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11-fx-similarity.R
\name{fx_sibling_report}
\alias{fx_sibling_report}
\title{Surface confusable type pairs through the taxonomy and the data}
\usage{
fx_sibling_report(
  sigs,
  paths,
  labels = names(paths),
  weights = SIGNATURE_WEIGHTS,
  path_near = 0.34,
  sig_near = 0.1
)
}
\arguments{
\item{sigs}{A named list (or matrix of rows) of signatures.}

\item{paths}{Ontology paths, aligned to \code{sigs}.}

\item{labels}{Row labels (default \code{names(paths)}).}

\item{weights}{Signature weights (default \link{SIGNATURE_WEIGHTS}).}

\item{path_near, sig_near}{Thresholds for taxonomic / signature nearness.}
}
\value{
A data frame of pairs with distances and a \code{status} column
(\code{both}, \code{hidden_risk}, \code{safe_split}, \code{unrelated}).
}
\description{
Compares every pair of types through two lenses -- taxonomic distance
(\code{\link[=fx_path_distance]{fx_path_distance()}}) and signature distance (\code{\link[=fx_signature_distance]{fx_signature_distance()}}) --
and flags where they disagree. \code{hidden_risk} pairs (far in the taxonomy but
near in signature space) are the dangerous set that needs hard negatives
banked against each other; \code{safe_split} pairs are siblings the data tells
apart easily.
}
\seealso{
\code{\link[=fx_path_distance]{fx_path_distance()}}, \code{\link[=fx_signature_distance]{fx_signature_distance()}}
}

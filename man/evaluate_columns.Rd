% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03-01-evaluate.R
\name{evaluate_columns}
\alias{evaluate_columns}
\title{Evaluate formex predictions against a labeled column corpus}
\usage{
evaluate_columns(
  columns,
  labels,
  crosswalk,
  headers = NULL,
  use_name = FALSE,
  ...
)
}
\arguments{
\item{columns}{A list of character vectors, one per labeled column.}

\item{labels}{A character vector of source-vocabulary labels, \code{length == length(columns)}.}

\item{crosswalk}{A data frame with columns \code{source_label}, \code{data_type},
\code{data_subtype}, \code{data_class} mapping each source label to a formex triple
(\code{data_class = NA} marks an out-of-scope label).}

\item{headers}{Optional character vector of real column headers (only used
when \code{use_name = TRUE}).}

\item{use_name}{If \code{TRUE}, predict with \code{\link[=guess_column]{guess_column()}} (values + name);
otherwise value-only via \code{\link[=guess_data_type]{guess_data_type()}}.}

\item{...}{Passed to the underlying guesser (e.g. \code{threshold}, \code{n_sample}).}
}
\value{
A data frame with one row per column: \code{label}, \code{gold_type},
\code{gold_class}, \code{pred_detector}, \code{pred_type}, \code{pred_class}, \code{source},
\code{in_scope}, \code{class_correct}, \code{type_correct}.
}
\description{
Evaluate formex predictions against a labeled column corpus
}
\seealso{
\code{\link[=eval_summary]{eval_summary()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/17-fx-triage.R
\name{fx_triage_field}
\alias{fx_triage_field}
\title{Triage a column for banking as a test case}
\usage{
fx_triage_field(x, expected_data_type = NA, features = NULL)
}
\arguments{
\item{x}{A column (any vector).}

\item{expected_data_type}{Optional \code{data_type} the corpus label implies
(e.g. \code{"number"}); enables the numeric-vs-text mismatch check.}

\item{features}{Optional precomputed \code{\link[=fx_extract_features]{fx_extract_features()}} row (avoids
recomputation in a batch).}
}
\value{
A list: \code{verdict} (\code{"garbage"} | \code{"empty"} | \code{"mismatch"} |
\code{"clean"} | \code{"hard"}), \code{keep} (bank it?), \code{reasons}, \code{difficulty},
\code{noise_markers}.
}
\description{
Classifies a field into a quality verdict before it costs a label. Corrupted
and empty fields are excluded (with reasons); label-vs-value mismatches are
banked but flagged; clean fields are difficulty-rated.
}
\seealso{
\code{\link[=fx_extract_features]{fx_extract_features()}}
}

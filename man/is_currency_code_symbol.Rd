% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dt-currency.R
\name{is_currency_code_symbol}
\alias{is_currency_code_symbol}
\title{Is it a currency code + symbol + amount?}
\usage{
is_currency_code_symbol(x)
}
\arguments{
\item{x}{A character vector of candidate values.}
}
\value{
A logical vector the same length as \code{x}: \code{TRUE} where both the syntax
matches and the prefix is a valid ISO 4217 code, \code{FALSE} otherwise, \code{NA}
where \code{x} is \code{NA}.
}
\description{
Tests whether each element is an ISO 4217 code immediately followed by a
currency symbol and a numeric amount with at most two decimals, e.g.
\code{"USD$12.30"}, \code{"EUR€299.10"}, \code{"JPY¥22"}, \code{"GBP£1222"}. The prefix
must be a real ISO 4217 code; the symbol must be one of \code{$}, \verb{€},
\verb{¥}, \verb{£}.
}
\details{
The code and symbol are not cross-checked for consistency (e.g.
\code{"USD€10"} passes), because market data frequently pairs an offshore symbol
with a settlement code.
}
\examples{
is_currency_code_symbol(c("USD$12.30", "ZZZ$1", "USD 10", NA))
#> TRUE FALSE FALSE NA

}
\seealso{
\code{\link[=is_currency_code]{is_currency_code()}}, \code{\link[=is_currency_symbol]{is_currency_symbol()}}, \code{\link[=guess_data_type]{guess_data_type()}}

Other currency detectors: 
\code{\link{is_currency_code}()},
\code{\link{is_currency_symbol}()}
}
\concept{currency detectors}

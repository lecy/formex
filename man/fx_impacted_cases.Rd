% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15-fx-ontology-diff.R
\name{fx_impacted_cases}
\alias{fx_impacted_cases}
\title{Map an ontology diff onto banked cases}
\usage{
fx_impacted_cases(diff, bank_dir, new_version = NULL, write = FALSE)
}
\arguments{
\item{diff}{Output of \code{\link[=fx_ontology_diff]{fx_ontology_diff()}}.}

\item{bank_dir}{Directory of schema-v2 case JSON files.}

\item{new_version}{If set, cases already at this \code{label.ontology_version} are skipped.}

\item{write}{If \code{TRUE}, write the two label flags into affected files.}
}
\value{
A data frame: \code{case_id}, \code{file}, \code{semantic_type_id}, \code{change_kind},
\code{case_impact}, \code{action}, \code{relabel_trigger}.
}
\description{
Matches diff rows to schema-v2 case files on \code{label.semantic_type_id} (never
on text), applies the depth rule (a case is affected only by changes at or
above the depth it was labelled to) and the version rule (already-migrated
cases are skipped), and optionally flags affected cases by writing ONLY
\code{label.needs_relabel} / \code{label.relabel_trigger}.
}
